# Llama-family sample (not Meta-gated). Method validation only.
stage: llama
repo: unsloth/Llama-3.2-1B-Instruct
revision: main
license: llama3.2 / see model card
attribution: >
  Llama-3.2-1B-Instruct weights via the unsloth mirror (BF16 safetensors).
  Meta's meta-llama/Llama-3.2-1B-Instruct is gated (401 without a license grant).
  This repo does not redistribute the weights.
block_sizes: [256]
min_bytes: 104857600
max_bytes: 167772160
include_embeddings: false
cache_dir: outputs/hf_cache
local_dir: outputs/models
output_dir: outputs/reports/llama
selection: complete_encoded_bytes
disclaimer: >
  Llama-family method validation only.
  Do not present these BPW numbers as 1–2 GB / 8 GB model evidence.
